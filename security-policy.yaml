apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-mobile-to-core
spec:
  podSelector:
    matchLabels:
      app: core-banking
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: mobile-gateway
